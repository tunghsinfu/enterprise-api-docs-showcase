server:
  port: 8080

spring:
  application:
    name: demo-swagger-ui

springdoc:
  swagger-ui:
    path: /swagger-ui/index.html
    enabled: true
  api-docs:
    path: /v3/api-docs
    enabled: true
